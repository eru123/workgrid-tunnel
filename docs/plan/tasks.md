- [x] Set up Cargo workspace with crates workgrid-protocol, workgrid-relay, workgrid-daemon, workgrid-cli
- [x] Define relay control-plane message types in workgrid-protocol (register, pair-request, pair-ack)
- [x] Implement relay: accept WebSocket connections, verify signatures against registry
- [ ] Implement relay: pair inbound daemon and client connections by server_id
- [ ] Implement relay: bidirectional byte forwarding between paired connections
- [ ] Add relay registry persistence (server_id -> public key) with add/revoke operations
- [ ] Write a runnable check that pairs two dummy WebSocket clients through the relay and confirms bytes flow both ways
- [ ] Add russh as a dependency to workgrid-daemon and bind an SSH server to a WebSocket stream instead of a TCP socket
- [ ] Implement daemon config loading (identity key, relay endpoint) from a fixed local path
- [ ] Implement daemon reconnect-with-backoff to the relay on disconnect
- [ ] Implement daemon SSH pubkey auth against an authorized_agents allowlist file
- [ ] Implement daemon allowlist reload on SIGHUP or file change without restart
- [ ] Implement daemon audit logging of channel-open and exec events with client key fingerprint
- [ ] Write a runnable check that a client can open two concurrent SSH channels to one daemon connection and get independent output on each
- [ ] Implement CLI bootstrap: plain SSH connect with host/user/key or password
- [ ] Implement CLI bootstrap: uname detection and matching static daemon binary selection
- [ ] Implement CLI bootstrap: SFTP push of the daemon binary to a fixed remote path
- [ ] Implement CLI bootstrap: Ed25519 keypair generation and remote config file write over the same SSH session
- [ ] Implement CLI bootstrap: relay registration call for the new public key
- [ ] Implement CLI bootstrap: systemd --user unit install with nohup fallback and watchdog loop
- [ ] Implement CLI bootstrap: poll relay for daemon-online confirmation, then close the plain SSH session
- [ ] Implement CLI bootstrap idempotency: detect already-installed same-version and no-op
- [ ] Implement CLI bootstrap upgrade path: detect older version, push new binary, restart, preserve identity
- [ ] Write a runnable check for bootstrap idempotency against a test container
- [ ] Implement CLI connect command using only the relay (no plain SSH)
- [ ] Implement CLI exec command with --json output mode returning stdout, stderr, exit_code
- [ ] Write a runnable check that exec --json output has no TTY/ANSI artifacts
- [ ] Implement CLI session new/list/attach/kill commands over multiplexed channels
- [ ] Implement CLI forget command: relay key revocation plus optional remote daemon teardown over plain SSH
- [ ] Implement interactive TUI with ratatui: tabs/panes for concurrently attached sessions
- [ ] Implement PTY resize forwarding as SSH window-change requests in the TUI
- [ ] Add static musl build configuration for workgrid-daemon and workgrid-cli
- [ ] Add an install script that bundles pre-built daemon binaries for each supported target arch
- [ ] Document relay deployment steps in docs/operations
- [ ] Document CLI usage (bootstrap, connect, exec, session, forget) in docs/user-guides
- [ ] Document daemon architecture and wire protocol in docs/architecture
- [ ] Verify end-to-end flow against the real skiddph server: bootstrap, connect, exec, multi-session, forget
